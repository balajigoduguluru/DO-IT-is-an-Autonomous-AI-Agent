{
  "buildCommand": "cd frontend && npm install && npm run build",
  "framework": null,
  "functions": {
    "src/api/**/*.py": {
      "maxDuration": 30
    }
  },
  "regions": ["iad1"],
  "env": {
    "PYTHONPATH": "."
  },
  "installCommand": "pip install -e ."
}