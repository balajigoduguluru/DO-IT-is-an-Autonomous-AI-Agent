{
  "buildCommand": "pip install -e .[ai]",
  "devCommand": "uvicorn src.api:app --reload",
  "installCommand": "pip install -e .[ai]",
  "framework": "fastapi",
  "functions": {
    "src/api/**/*.py": {
      "maxDuration": 30
    }
  },
  "regions": ["iad1"],
  "env": {
    "PYTHONPATH": "."
  }
}